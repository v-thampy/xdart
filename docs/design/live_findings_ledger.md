# Live findings ledger — maintainer-reported display bugs

**Rule:** every display-fix agent updates the Status column in the SAME commit as its fix; the
orchestrator checks this ledger at every review; the maintainer's live session verifies every
`fixed-unverified` row before anything is called done. Do NOT close a row on tests alone —
`live-verified (date)` requires the maintainer's repro.

| ID | Symptom (maintainer's words) | Repro | Status |
|----|------------------------------|-------|--------|
| OV-1 | **When Overlay is first selected, the selected trace gets ERASED when the next trace is selected.** After that it accumulates normally for a while. NOT hydration-dependent — happens with recent, adjacent frames. | Mode → Overlay with frame A displayed → click frame B → A vanishes, only B; C/D then accumulate | fixed-unverified (round-3: seed payload-owned overlay history on Overlay/Waterfall entry; verify Session-1 A3) |
| OV-2 | Selecting a frame much older than current → whole plot redraws WITHOUT the old traces (one trace) | Overlay w/ many traces, paused live run, click frame outside the 64-window (e.g. 331 @ current ~576) | fixed-unverified (round-3: unchanged normUpdate no longer wipes accumulator; monotonic evicted-selection test; verify Session-1 A3) |
| OV-3 | Hydrated frames fresh-plot instead of overplotting | Overlay, select evicted frames, wait for hydration | fixed-unverified (`8d35a37c` scheduler; verify at Session-1 item A3) |
| OV-4 | Overlay clears when the CURRENT frame is evicted during live | live run past 64 frames, Overlay + Auto-Last | fixed-unverified (`59d1f8f1`/`30ecf58a`; verify at Session-1 item C3) |
| OV-5 | Empty selection/control repaint wipes Overlay accumulator | Overlay with traces → deselect all/whitespace → plotUnit/slice/imageUnit/run-end repaint | fixed-unverified (H7 commit 1: history gate hoisted above empty-selection return; 2D clear keep-last; slice reset-key/reintegrate/all-NaN guards; verify Session-1 A3/A4) |
| FS-1 | Beachball on fast shift+arrow sweep (Single + Overlay) | ~800-frame scan, hold shift+arrow | **live-verified fixed** (2026-07-02, maintainer: "beach ball problem appears to be solved"; commits `84393c00`+`8d35a37c`) |
| FS-2 | Floating empty 1D-plot window pops up during sweeps | sweep across the 15-curve auto-waterfall boundary | fixed-unverified (`84393c00` detach/attach helpers; verify Session-1 A1) |
| FS-3 | Raw image bounces between frames after fast multi-select | fast multi-select, then wait | fixed-unverified (`8d35a37c` generation scheduler; verify Session-1 A4) |
| RN-1 | Run aborted mid-scan: writer `r+` open failed while hydration read held the file | scroll to evicted frames during live run | fixed-unverified (`084f3410` file_lock serialization + S-12 late-writer audit: reintegrate shadows and NeXus final save now take `file_lock` before pausing the H5 pool; verify Session-1 A2) |
| MS-1 | Frame-count reconciliation: dispatch=288 / processed=287 / post-live indexed=283 (2026-07-02 00:51 log) | pause mid-run, read the post-live index line | fixed-unverified (MS1/H1: close compares submitted vs written, cancel logs durable count, dispatch counter counts accepted frames, record-store persistence is mode-scoped) — **re-observed 2026-07-05 on a CANCELLED run** (indexed=2099 processed=2102, 3-frame gap): on Stop this is the EXPECTED in-flight tail (frames processed but not yet flushed when the run was torn down), NOT loss. **SHIP GATE narrowed:** run ONE scan to COMPLETION (no Stop) and confirm the "post-live indexed fewer frames than processed" warning is ABSENT (indexed==processed). A gap on a COMPLETED run is real data loss and reopens this. |
| LN-1 | 2026-07-05 live-run (3621-frame GIWAXS overlay) noise + crash: (a) `TypeError: float - NoneType` in `_subtract_if_shape_matches` on a repaint with no background set (guard lost in the BR-3 quarantine reset); (b) ERROR-level "indexed but has no integrated data" spam while a run was actively writing (browse read-ahead of not-yet-flushed rows); (c) repeated "suppressing repeated hydration requests … after 3 failures" WARNING spam; (d) reduction-worker log line repeating on streaming-session reuse | long overlay run with no BG set; Stop mid-scan; new-scan startup browse | fixed-unverified (`2a2f2a13` None/scalar background no-op + scalar-data warning; `f862249c` missing-row ERROR→DEBUG during active rewrite, append browser seeded list-only, transient flag propagated across all series rebuild seams; `fca22bd8` hydration-suppression warning→debug; worker line no longer repeats on session reuse. Production-wired: `test_persist_before_evict` idle-ERROR vs active-DEBUG, `test_live_repaint_without_background_no_exception`; verify Session-1: long overlay run, no BG → no exception + quiet log) |
| CFG-1 | Custom mask silently not applied when its shape ≠ detector: 3072×3072 mask vs Eiger 2167×2070 (2026-07-05 live log) | load a mask whose shape ≠ the detector, run | not-a-bug / user-config — the refusal is correct defensive behavior (won't mask wrong pixels) and is surfaced as a WARNING. Session-1: confirm the intended mask matches the detector for the GIWAXS workflow, or the custom mask is silently absent from every frame. |
| LD-1 | Stale `_load_coalesce_timer` from file A can fire after switching to file B, letting A-frame chunks populate the just-cleared viewer caches | arm disk-load debounce, switch processed file before it fires | fixed-unverified (Wave-2: cancel load debounce at set_file/data_reset/mode cleanup; stale-debounce regression test; verify no wrong-file render in Session-1) |
| BR-1 | **Clicking a processed .nxs in the data browser clears ALL plots immediately. They should persist until a FRAME of the new scan is clicked.** (Re-raised 2026-07-02 — reported earlier, not yet addressed. The deferred-reset `_browser_scan_reset_pending` was built for exactly this, but something still clears on file select — likely set_file's listData clear → empty shared frame_ids → EMPTY render, the OV-5 family, which blanks raw/cake/1D regardless of the deferral.) | Data browser → click a different processed .nxs → all panels blank before any frame is selected | fixed-unverified (Wave-2: browser-pending `set_data` returns without repaint until a real frame is selected; first-frame reset restores/reloads selection; verify Session-1 BR-1) |
| PF-1 | **Append on a fully-processed 651-frame Eiger file takes ~18 s with zero frames to process** (read-then-check: every already-done frame is read+decompressed before the skip) | Append/resume a completed scan; `Total Files Processed: 0` yet `Total Time: 18.06s` | fixed-unverified (PF hotfix: the skip snapshot is now primed once at run start with `mode='r'`, per-frame checks are in-memory only, failures degrade to empty, and all-skipped Append still reloads/selects the finished scan; verify Session-1 append retry) |
| PF-2 | APS/QXRD single `.tif` with dash index silently enumerates zero frames | Image Series pointed at `..._d1200-00000.tif`; output reports zero processed / no useful failure | fixed-unverified (PF commit + S-8: `[_-]<digits>` suffix parser for image/container names, parsed `(scan,index)` at-or-after filter instead of lexicographic path compare, and zero-discovered runs warn with directory/ext/pattern details; verify APS_Zoe repro) |
| GI-1 | **Auto Q/χ ranges with χGI as the integration axis produce a jumbled/wrong 1D profile; explicit −180..180 gives the expected result.** P0: WRITTEN data differed, not just display (auto used pyFAI's implicit geometry extent). Standard-mode χ had the same class. | GI mode, 1D axis χGI, Auto range vs explicit −180..180 (APS_Zoe SiO2 + Perkin poni) | fixed-unverified (`2e278aa2`: auto ≡ explicit −180..180 byte-equal, both modes; frozen ranges re-key on mode switch; verify at Session-1 — re-run the χGI auto vs explicit comparison live) — **reopened-by-review (S-4): the STANDARD-mode sibling still failed through the REAL GUI pipeline** — chi_offset (default 90°) shifted the 1D INPUT range but the 1D chi OUTPUT axis stayed in the raw pyFAI frame 90° out of frame with the 2D cake chi (the 2D re-adds the offset, the 1D never did); the byte-equality tests call the integrate functions directly and missed it. → **fixed-unverified** (`c79a7434` output relabel + `f52de457` input-shift/plan-builder completion + `4ee01755` q/2θ wedge fix: `Integration1DPlan.azimuth_offset` mirrors the 2D — carry chi_offset, shift explicit input ranges by −offset, re-add it to the 1D chi OUTPUT axis in reduction; `test_non_gi_chi_reduction_reapplies_chi_offset_s4`, explicit-partial tests, q-wedge test, and readiness tests verify 1D↔2D CONSISTENCY headlessly. **SHIP GATE: real-data χ validation against the team reference via `scripts/g18_s4_chi_validation.py` — a written-data frame fix must not land blind; post-1.0 is too late to change bytes.**) |
| CF-1 | **Append after changing the processing config no-ops but relabels the loaded data with the new panel units.** | Load a Standard processed scan → flip Experiment to Grazing → Run in Append | fixed-unverified (headless Append config comparison now feeds the GUI confirmation reason without disabling Run; Controls V2 populates `processed_config` from the loaded same-target scan and reaches the shared Yes/No modal; display axes use the stored data config until Replace/new data stamps a new one; verify Session-1 Append Standard↔Grazing) |
| CF-2 | **Append config mismatch did not surface at Run click for Eiger `_master` targets; writer backstop raised mid-run instead.** | Load Standard Eiger `.nxs` → flip panel to Grazing → Run in Append | fixed-unverified (Eiger-aware target identity strips `_master`; GUI Run click shows Yes/No overwrite modal, No does not run, Yes flips to Replace then runs; BW-A2 additionally hardens Append target load/first-flush integrity so degraded loads preserve the existing file and loaded rows are treated as already persisted; BW-A3 adds the thread-side initialize-scan config guard before frames process; verify Session-1 Append Standard↔Grazing + G8 degraded-load drill) |
| CF-3 | **Fresh xdart launch + partial Standard target on disk + GI panel + Append skips the modal and lets the writer guard abort mid-run.** | Fresh launch, no viewer-loaded scan, existing partial Standard `.nxs` target (e.g. 210/651), panel set to Grazing, Run in Append | fixed-unverified (run-click predicate reads the append target file's stored processing config pre-run when no loaded scan fast path applies; worker initialize also reads the target config and fails loud before any frame processes, including live/cold empty-`img_file` paths; No runs nothing and leaves the file untouched; append-axis writer backstop no longer emits a duplicate traceback; verify Session-1 fresh-launch partial-target Standard→Grazing) |
| UI-3 | **χ (c/w) + Pin can stay disabled after a processed append/browser load even when 2D cake data exists.** | Load/append processed Int 2D data, inspect slice controls after cake draw | fixed-unverified (slice controls now resync on cake data presence and on no-cake update/clear, independent of panel GI state; verify Session-1 F5-adjacent and append-load path) |
| OV-6 | **Overlay must survive scan boundaries (cross-scan comparison = the original design).** Contract (maintainer, 2026-07-02): in Overlay mode ONLY the Clear button, or a new scan with a DIFFERENT integration axis or npt, may clear the 1D plot. Compatible grid ⇒ new scan's traces APPEND. Pure data-browsing behavior deliberately left open — maintainer judges live. | Run/load scan B (same axis+npt) after overlaying scan A → A's traces currently vanish | fixed-unverified (OV-6: grid-keyed reset identity, scan-qualified row ids, trace-number waterfall y-axis, compatible browser first-frame path; verify Session-1 F7/F8) — **reopened-by-review (BL-6): a compatible cross-scan append plotted traces at the WRONG x** (`ref_x` seeded from the new scan's grid, not `prior.x`; the accumulator reinterped only on sample-COUNT mismatch, so two scans with the same axis+npt but a different `radial_range` — the very cross-scan-comparison workflow — rendered scan B's intensities at scan A's x). → **fixed-unverified** (`552365eb` + `5a12f096`: seed `ref_x = prior.x` when the prior is grid-compatible + unit-unchanged; `accumulate_waterfall` reinterps when sizes match but values differ; S-17 empty-grid publication no longer wipes the accumulator; production-wired `test_bl6_overlay_xgrid.py`; verify Session-1 F7/F8 with a recalibrated range) |
| OV-7 | **Overlay multiple slice cuts from the same scan+frame.** Slice center/width must distinguish pinned cuts without forcing a reset; the current unpinned cut must mutate in place while Pin freezes recipes into the overlay. | In Overlay, enable χ/q slice, Pin two centers on one frame, move the live c/w, then norm/BG/unit repaint | fixed-unverified (OV-7: slice params moved from reset key to projection row identity; Pin button + Cmd/Ctrl+P; recipe registry rematerializes pinned rows; R1 restyles the live current cut as palette-neutral gray/lightweight and keeps pinned colors in pin order; verify Session-1 texture-cut workflow) — **reopened-by-review (OV-7b, maintainer): Pin did not ABSORB the live current cut** — the current uses a SENTINEL projection_id, so it never matched a pin and rendered a duplicate trace + an extra offset slot when its c/w equalled a pin (repro: pin χ=18 ⇒ three traces pin@1/current@18/pin@18, should be two). → **fixed-unverified** (`67e115e9`: the current renders only while its CONCRETE (axis,center,width) projection_id differs from every pin — suppress-when-equal; the absorbed sentinel row is DROPPED from the accumulator via a new `accumulate_waterfall(drop_ids=…)`; (c) the live cut restyled to a dashed LINE only — markersize 0, 75% opacity, palette-neutral gray, "· current"; `test_overlay_current_cut_absorbed_by_matching_pin_ov7b`; verify Session-1: pin ⇒ 2 traces not 3, spin c/w ⇒ current reappears, style is gray/dashed/75%) — **OV-7c residual fixed-unverified (`0bc21183`):** live current now drops any prior transient sentinel before repaint so it always previews the next free offset slot above pins; Pin absorption leaves the new solid pin in that same slot, the next current appears one slot higher, and the Pin button stays disabled in Single mode (`test_ov7c_live_current_uses_next_pin_slot_and_pin_freezes_in_place`; verify Session-1 G3/G13 no-overlap/no-jump) |
| MEM-1a | **Live-mode OOM (CRITICAL): the wrangler→GUI `_published_frames` hand-off is uncapped and live raw is never freed on staging eviction, so a GUI thread that falls behind the producer pins one ~64 MB (float64-upcast) LiveFrame per undrained frame → 30–60 GB → crash.** | sustained fast live run where the GUI drain lags the reduction producer | fixed-unverified (BW-A1: `_BoundedFrameHandoff` drop-oldest cap remains bounded and now tolerates the GUI `pop(idx, None)` racing eviction; `free_raw` on `LiveFrameSeries` staging eviction; verify live: long fast run peak RSS stays bounded, dropped labels re-hydrate) |
| MEM-1b | **GI dropped-2D cakes pinned forever: a publication-gate-dropped `('2d',mode)` is excluded from `mark_persisted` but its heavy `intensity_2d` is never released, so `_label_heavy_payload_persisted_locked` can never evict it → hundreds of full cakes on GI scans, no ceiling.** | GI scan with a class of all-dummy 2D rows below the critical angle | fixed-unverified (reopened by prerelease review S-1, fixed in BW-A1: `LiveScan._save_to_nexus()` propagates the real writer dropped-mode report, and `QtNexusSink` production save path marks dropped 2D modes instead of persisted; verify live: GI-scan RSS bounded, reload → 1D present / 2D ABSENT) |
| MEM-1c | **Series-average + Append silently writes NOTHING when the averaged output already exists** — every source frame collapses to output #1, which `_should_skip_before_read` skips, so the whole run yields no output with only a benign INFO line. | series-average Append into a `.nxs` that already holds the averaged frame 1 | fixed-unverified (run-start pre-flight refuses with an actionable message instead of skipping; `tests/xdart/test_append_skip_before_read.py`; verify live: re-run series-avg Append shows the block, not a silent no-op) |
| MEM-1d | **Image-viewer Cmd+A select-all disables the raw LRU: the keep-set covers the whole selection, so eviction finds no non-kept victim → ~64 MB/frame hydration until OOM from one keystroke.** | Image Viewer on a 5000-frame stack → Cmd+A | fixed-unverified (LRU cap WINS over the keep-set — the oldest kept label is evicted so residency stays ≤ cap; `tests/xdart/test_mem1_oom_bounds.py`; verify live: select-all on a large stack stays bounded) |
| PR-1 | 651-frame Eiger live baseline regressed 24-26s -> 33.3s (2026-07-02) | live 651 Eiger Int 2D run | closed-environmental — baseline recovered on re-run, no code fault (see `codex_tasks/perf_regression_651.md`); re-confirm ~25-26s at Session-1 MEM-3 baseline check |
| MEM-3 | default 20-worker reduction pool wastes ~3 GB peak vs the measured knee (4); UI Cores knob did not govern the pool | 651 live run, watch peak RSS vs worker count | fixed-unverified (ported: reduction_worker_cap knee=min(4,cpu), RAM floor 2, honest Cores→pool, XDART_REDUCTION_WORKERS, unbounded fallback pools capped; incl. MEM-2 _heavy_staging_window spine fix and S-11 PublicationStore heavy-window resize at streaming-session create/reuse; verify Session-1: 651 live baseline ~25s at default) |
| UI-6 | **Multi-select >~70 frames: display oscillates ("blinks") between two render states.** No crash/beachball/memory growth — two repaint authorities alternating (multi-select aggregate vs current-frame?), threshold suggests compute time crossing a refresh-timer period. | Image mode, select 70+ frames, watch the 2D panel | open (live-found 2026-07-04 stress test; maintainer-accepted for v1.0; v1.1: diagnose with XDART_PERF + generation logging before fixing — render-authority class, H30 family) |
| PERF-3 | **Waterfall+overlay degrades beyond ~400 traces on a 3600-frame live run** — display accumulation lags, never finishes, then backpressure slows PROCESSING too (display→handoff→writer coupling). | Live 3600-frame run, Overlay+waterfall on from frame 0 | fixed-unverified (BW-A6: `accumulate_waterfall` now uses a geometric row buffer instead of rebuilding the stack on every append, and overlay 1D hydration bursts compact into bounded ordered queue entries; verify Session-1 1600-live + 2600/3621 Show All + RSS/stall behavior) |
| BR-2 | **Rapid frame-stepping in Overlay skips frames once outside the resident 1D window (512): superseded hydrations are generation-cancelled and never join the accumulator.** | Overlay on, click down-arrow rapidly through a 3600-frame scan | fixed-unverified (BW-A6/BR-2b: live 1D record residency is now byte-budgeted from run-start npt with a ≥4096 floor, and overlay-1D selection-superseded hydrations complete-and-append in click order while reset/scan-switch cancels stale work; verify Session-1 12/21-gaps repro) |

**Acceptance test that covers the OV family (round-3/H7 handoff):** accumulator count is
MONOTONIC through every step of: Overlay-mode entry (seeded with the displayed trace) →
resident click → evicted click → deselect-all → unit toggle → hydration completion → repaint.
Only `Clear`, an incompatible grid/source (`reset_key`), reintegrate-finish, or a REAL norm-channel
change may reset it. CROSS-SCAN (OV-6): a scan boundary with a COMPATIBLE grid (same integration axis kind + npt) must NOT reset — only Clear or an incompatible grid may. SLICE IDENTITY (OV-7): slice center/width now live in row identity for pinned cuts, while the reset key keeps only projection axis kind + npt + 2D source.

## BL-6 incompatible-grid guard (`a7851fc3` on `laneB-blocker-wave`) — **OBE, do NOT port** (2026-07-04)
The adversarial review found `test_overlay_waterfall_payload_accumulates_in_payload_across_renders`
red on `laneB-blocker-wave`: an incompatible grid (npt 5 → 7) APPENDED instead of RESETTING because
my BL-6 `prior_x_seed` adoption force-interped the frame onto the prior grid without a size check. I
fixed it on my branch in `a7851fc3` with an `x.shape == prior_x_seed.shape` seed guard.

**Resolved by PROOF against integrate HEAD, not just my branch.** Branched a worktree off
`feature/remediation` HEAD `93997bea` (which carries codex's `6f0b726f` `append_row` restructure AND
the ports of my S-14/S-16/BL-6) and ran the npt-5→7 reset test there with `PYTHONPATH=<worktree>/src`
(defeating the editable-install-points-at-MAIN trap): **it PASSES** (`test_aggregation_wiring` 42✓,
`test_frame_publication` 75✓; the `p6.plot_history.ids == (('scan3',4),)` reset assertion holds).
HEAD's `append_row` computes `row_grid_key` from the ACTUAL frame and gates `use_prior_seed` on
`overlay_grid_keys_match(prior.reset_key, row_grid_key)` — a full-grid-key guard that SUBSUMES my
`x.shape` check, so the incompatible frame never adopts the prior seed and the reset fires.

⇒ The regression does NOT exist at HEAD; the port ordering is safe. `a7851fc3`'s
`display_publication.py` hunk (the `x.shape` guard + the S-16 comment re-scope) is **OBE** and
conflicts structurally with the reshaped site — **do not port it**. No code change is needed at HEAD;
the ported test already covers and enforces the reset.
