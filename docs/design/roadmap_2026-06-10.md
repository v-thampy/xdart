# XRD Toolkit Roadmap — 2026-06-10

Two coupled repos: **`ssrl_xrd_tools`** (headless analysis/IO core) and **`xdart`** (thin Qt live-experiment
GUI over it). This is the forward-looking source of truth; per-task detail lives in the other `review/`
docs and in each repo's `CLAUDE.md`.

## North star (long-term goals)
1. **Headless-first ssrl APIs** — the analysis core is fully usable with no GUI (notebooks, scripts, future
   services). `ssrl_xrd_tools` imports **no Qt**, ever.
2. **Thin xdart** — the GUI is a thin shell over the core; anything not Qt-specific belongs in ssrl.
3. **Robustness** — fail-loud on data-writing errors, strict schema validators, correctness spines
   (live ≡ batch ≡ reload), no silent-wrong-data.
4. **Performance** — streaming, parallel reduction that scales; bounded memory; live browsing without
   freezes.
5. **Expandability** — clean seams so new data sources, analyses, and viewers slot in **additively**
   without touching the core.

## How Architecture V2 delivers on these
- **One streaming reduction spine** (`ssrl reduction.ReductionSession`: parallel worker pool + single
  writer thread, bounded in-flight, fail-loud `finish()`) → *performance* (live/batch both stream by
  default; live 2D ~27.5 s vs 59–76 s serial) and *robustness* (write failures raise, never silently
  "succeed").
- **Source + sink abstraction** (`sources/` + `open_source(spec)`; `ReductionSink` protocol with xdart
  injecting its Qt `QtNexusSink`) → *headless-first* (ssrl runs the spine with no Qt) and *expandability*
  (new sources/sinks are additive; ssrl never imports xdart).
- **N1 portable raw-source paths** (relative `source/path` + `entry/@source_base`, resolved in the ssrl
  readers with a `source_root=` override) → *robustness/portability* (a processed `.nxs` resolves its raw
  after the data moves machines).
- **xdart display layer** (Qt-free `display_logic` decision core + keyed `PanelKey`/`layout` descriptors +
  registered controllers) → *thin xdart* and *expandability* (Stitch/RSM viewers register panels; the core
  never imports them).
- **GI whole-scan grid freeze, strict writer validators, the live≡batch≡reload spine** → *robustness*.

## Current status (Jul 2026)
The two-repo architecture-v2 work has been folded into the monorepo
`/Users/vthampy/repos/xrd-tools`; Architecture V2 shipped into the current line.
The streaming reduction spine, source/sink contracts, N1 relative source paths,
Project/Save Path controls, schema-as-code foundation, byte-compat gates, strictness
policy, and release preflight are all in-tree.

Controls Panel V2 is now the default live surface. The Int flip is **code-complete
and default-on**: native Int state is authoritative for Run/Reintegrate/session, the
V2→legacy write-through bridge is retired, threshold/Average Scan/reintegrate parity
regressions are fixed, and the freeze/refresh saga has code/test coverage. The
remaining panel-v2 release gate is the maintainer's manual live checkpoint.

The pre-v1 long pole is now the greenfield remediation **A** stream: wire the
`FrameRecordStore`, make display reads store-first, move eviction ownership, then
retire the `data_1d/data_2d` mirrors. The headless Source-card/provenance/RSM-Stitch
page work remains post-v1 unless it is purely contract-level and disjoint from the
display/store collapse.

## Deferred to next cycle (explicitly scoped out of this release)

### Jun 10 codex-review residuals (triaged)
- **GI unverifiable-source grid freeze — RESOLVED as policy (T0-4, Jun 9).** Vivek's call (see memory
  `scan-taxonomy-gi-grid-policy` + `CC_postreview_plan_jun2026.md`): frames bin natively onto the common
  grid (values inside are exact) and incidence varies too little for cropped extreme tails to matter, so
  unverifiable sources (named-motor Eiger, Image-Directory) **warn and proceed** on the first-chunk
  freeze — the advisory is persisted in `/entry/reduction/config/gi_freeze_diagnostic`.  The free
  whole-scan union sweep stays for Image-Series; only an actual freeze ERROR aborts.  Source-aware
  whole-scan incidence enumeration is demoted to a nice-to-have.
- **Cancellation/teardown hardening (#2 + #4, small pre-release pass).** `submit()` blocks on the in-flight
  semaphore without polling cancel; terminal `finish()` does an unbounded writer-join — so a truly stalled
  worker can wedge Stop/Pause/close. Fix: timed-acquire loop polling `cancel_token` in `submit()` + a
  finish/abort timeout for GUI-owned sessions (+ a backpressured-cancel test). Robustness, not correctness.

- **#7 chunked executor retirement** — kept one cycle as the byte-identity *oracle* + fallback; retire as a
  focused PR (migrate the GI matrix rig chunked→streaming, then delete `_dispatch_batch_parallel` +
  `_BATCH_EXECUTION`). Keep `_process_one`/serial (true-live + no-cached-integrator fallback).
- **FIFO writer head-of-line** (perf only, bounded by `inflight_max`) → completion-queue/callback.
- **RSM**: real synthetic `run_rsm` test + energy-sentinel fix (#78) — do with RSM feature work.
- **Metadata models**: DECIDED keep-separate — `ScanMetadata` (scan-level) and `HeterogeneousMetadata`
  (per-frame) are orthogonal granularities, **not** a collapse.
- **N1 forward seams**: Tiled source-kind selector + embed-raw sink flag — build when Tiled/Bluesky is the
  active feature (seam shape already designed).
- **Example-notebook refresh** — read processed NeXus via the convenience readers; plotting, stitching,
  RSM, advanced analysis + interactive widgets.
- **Feature work**: stitching (1D+2D) UI, RSM viewer (PanelKey render extensibility WS-X2/#69), NeXus
  viewer mode; polish (#74 scan-list mtime off GUI thread, #76 legacy GI helper relocation).

## Seams to build future work on
- **New source** → ssrl `sources` `FrameSource` + `open_source(spec)`; xdart wrangler = thin adapter.
- **New analysis** → ssrl `analysis` plan (the `run_stitch`/`run_rsm` pattern) on the reduction/io core;
  headless-tested with small synthetic inputs (not monkeypatch).
- **New viewer/panel** → xdart `display_logic` `PanelKey` + `layout` descriptor + a registered controller;
  the pure core never imports the new module.
- **Persistence** stays the NeXus stacked-v2 schema (additive, back-compat) written through a `ReductionSink`.

---

## Deep review (Fable, 2026-06-09) — next-cycle items
Full detail (historical — superseded by the current review chain: `review_2026-06-15.md`, `review_v1_readiness_2026-06-18.md`). Release-gate items are in `CC_prerelease_gate_jun2026.md`; the rest:
- **Complete-v2-record orchestration into ssrl** (S3 + C2 + C3, one arc, high value): move per-frame
  source-ref / thumbnail / per-frame-geometry / provenance assembly + `write_frame_source_ref` (+ `@source_base`
  stamp/conflict) + the row-aligned-dataset drop helper into `ssrl io/nexus.py`; xdart's sink becomes
  Qt-signal-only. Makes a *headless* run produce a complete v2 file (live≡batch≡reload holds for non-GUI
  writers too) and lets ssrl tests cover the real on-disk contract instead of hand-built fixtures. **This is
  also step 1 of greenfield Difference 2 (below) — do it with that destination in mind.**
- **Reader-side schema-version check** (C1): `read_scan`/`open_scan`/`read_frame_view` warn (or raise behind a
  flag) when file `ssrl_schema_version` > supported. Trivial; do alongside the above.
- **Streaming hardening**: writer-loop `else:`-clause exception wrap so a callback exception can't kill the
  writer → submit deadlock (S1); `_products` retention knob — currently retains every 2D result unbounded,
  ~14 GB on a 10k-frame 2D scan (S2); atomic-mode finish-failure keeps a `.partial` instead of unlinking the
  whole run (S7). (FIFO head-of-line #3 also here.)
- **Cross-repo enforcement**: startup min-ssrl-version assertion + a test asserting it == the pyproject floor
  (C4, because editable installs bypass pip floors); a `tests/test_sink_contract.py` in ssrl driving a
  streaming session against a stub six-method sink incl. which-thread-each-hook (C7); a minimal CI matrix
  (ssrl full; xdart `-m display_logic` + sink/adapter subset vs ssrl head).
- **Collapse parallel representations**: finish publications-as-sole-display-contract (X1); retire or
  productionize `LiveFrame.integrate_*` (~280 LOC parallel GI dispatch kept only as the equivalence-spine
  reference, X2) + add a `_process_one`-vs-sink equivalence test; unify the two LiveScan→headless adapters
  (`LiveScanFrameSource`/`open_source` are dead at runtime, C5); re-point xdart `two_d_kind_from_units` at the
  ssrl one (C6); delete the ~275-line dead legacy `Frame/Scan` block in `reduction/core.py` (S4); rename
  `io.read.Scan` → `ProcessedScan`/`ScanHandle` before the API freezes (S5).
- **Smaller**: SWMR write path fix-or-remove (advertised, likely broken — S6); monitor-normalization skip warns
  (S8); energy None-not-NaN sentinel (#78/S9); profile the suspected O(N²) `scan_data` `.loc` enlargement on
  the GUI thread during B3/B4 (X4); `viz/__init__` lazy matplotlib/plotly import; per-frame file-reopen O(2N)
  grouping; the already-planned `io/nexus.py` + `image_wrangler_thread.py` + `h5viewer.py` module splits;
  dependency audit (ipykernel/xrayutilities/silx/joblib in runtime deps; stray prints).

## Strategic direction (greenfield, 2026-06-09) — NOT this release
Full detail (historical — executed via `CC_greenfield_implementation_plan_2026-06-12.md`). The honest finding: a from-scratch design would differ less in
*architecture* (contracts, pure display core, equivalence spine all reappear) than in *infrastructure* —
where code lives, who owns the data, what's enforced mechanically. Differences, with retrofit verdict:

1. **★ Monorepo / workspace (Vivek's chosen direction — "maintaining 2 repos is a pain").** One repo,
   `packages/xrd-core` + `packages/xdart`, one lockfile, one test invocation, one CI matrix, atomic cross-package
   commits; ordered PyPI publish becomes a script not a runbook; `review/` becomes versioned `docs/`. Deletes
   whole finding classes (C2 schema-split, C4 floor-bypass, C7 cross-repo CI). **Retrofit:** thin workspace repo
   with both as submodules + shared CI, or at minimum mirrored GitHub Actions (xdart CI checks out ssrl head;
   ssrl CI runs xdart's seam tests); move `review/` into the repo(s). Plan this as its own effort after the
   release.
2. **Boundary at data-ownership, not Qt — a headless `xrd-session` layer** owning the live scan state machine
   (start/pause/resume/stop), the single writer, save cadence, eviction, publication store; xdart becomes a pure
   *view*. Makes live≡batch≡reload a property of *construction* (one path) and "thin xdart" *structural* (no API
   to own data). **Step 1 = the deep-review S3/C2 move above** — do that with this destination in mind. The
   biggest architectural steer; QtNexusSink + ReductionSession is already two-thirds of it.
3. **One `FrameRecord`, one store** (collapse ScanFrame/LiveFrame/FrameView/FramePublication + data_1d/data_2d/
   PublicationStore). Already converging via X1/X2 — finish it; target: grep "keep both stores in sync" → nothing.
4. **Working store (zarr) + NeXus export** — separate the live append target from the archival file (kills the
   shared file-lock/h5pool/SWMR machinery). **Defer** — current locking works; revisit with Tiled/Bluesky.
5. **Schema as code** — one declarative `SCHEMA` table in core (datasets/dtypes/row-aligned flags/capability
   attrs/version) that the writer layout, reader expectations, validators, and fixtures all derive from. **High
   leverage, cheap, retrofittable** — start with a row-aligned table + the C1 version check; grow opportunistically.
6. **CI + contract tests from day one** (same as C4/C7) — cheapest counterfactual; the lettered review cycles
   were the manual equivalent.
7. **Day-one policies for new code**: domain naming (no `Live*`/two `Scan` classes); xarray as the result-path
   currency; one coalescing idiom (throttle); `None` as the only missing-value sentinel; strictness flags
   default-loud for headless, GUI opts into graceful degradation.

## Pre-ship sweep trade-offs (Jun 10, deferred to post-release — none block default path)
From the final pre-merge sweep (CC). All post-release; default streaming workflow unaffected.
- **M4 — Reintegrate-All RAM (HIGH post-release perf).** All N frames stay resident during a reintegrate-all
  (~10 GB on the 651-frame Eiger) — can OOM a small machine. Fix: periodic replace-saves inside the reintegrate
  loop so eviction can run (a save-cadence change on the recovery path; do carefully).
- **data_1d thumbnails (~262 KB/frame, ~3 GB/10k).** Functional (Raw preview reads them); capping changes
  old-frame preview behavior. Per-scan bounded. Decide a cap vs on-demand rehydrate post-release.
- **PublicationStore unbounded metadata (low).** Cake's only render source; capping could blank old-frame
  cake re-display. Post-release.
- **Serial-fallback (`XDART_LIVE_EXECUTION=serial`) + Int 1D (XYE) writes ZERO output** — buffer never
  flushes. NON-default opt-in only (default streaming + true-live both write XYE fine). Silent-data-loss on
  that escape-hatch path → make it **fail loud (warn/refuse)** or fix the flush before anyone relies on it.
- Refuted in this sweep (documented, do NOT "re-fix"): per-run wrangler-thread leak, set_wrangler
  accumulation, H5FilePool race, GI-2D freeze hole — all traced to unreachable paths.
